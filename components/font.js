import { Bebas_Neue, Koulen } from 'next/font/google';
export const bebas = Bebas_Neue({
  subsets: ['latin'],
  variable: '--bebas-neue',
  weight: ['400'],
});

export const koulen = Koulen({
  subsets: ['latin'],
  variable: '--koulen',
  weight: ['400'],
});
